(function(){const $=e=>document.getElementById(e);const stepDb=$("step-db");const stepAdmin=$("step-admin");const panelDb=$("panel-db");const panelAdmin=$("panel-admin");const statusEl=$("setup-status");const dbForm=$("db-form");const dbName=$("db-name");const dbUser=$("db-user");const dbPass=$("db-pass");const adminForm=$("admin-form");const adminName=$("admin-name");const adminPass=$("admin-pass");const btnAdminBack=$("btn-admin-back");function setStatus(msg,type){statusEl.textContent=msg||"";statusEl.className="status"+(type?(" "+type):"")}function gotoStep(n){if(n===1){stepDb.classList.add("step-active");stepAdmin.classList.remove("step-active");panelDb.classList.add("panel-active");panelAdmin.classList.remove("panel-active")}else{stepAdmin.classList.add("step-active");stepDb.classList.remove("step-active");panelAdmin.classList.add("panel-active");panelDb.classList.remove("panel-active")}}dbForm.addEventListener("submit",async e=>{e.preventDefault();const name=dbName.value.trim();const user=dbUser.value.trim();const pass=dbPass.value.trim();if(!name||!user||!pass){setStatus("请填写数据库名称、用户与密码","error");return}try{setStatus("测试数据库连接…");const resp=await fetch("/api/setup/database",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({db_name:name,db_user:user,db_password:pass})});const d=await resp.json().catch(()=>({}));if(!resp.ok){setStatus("数据库配置失败："+(d.detail||d.error||resp.status),"error");return}setStatus("数据库已配置","ok");gotoStep(2)}catch(err){setStatus("请求失败","error")}});btnAdminBack.addEventListener("click",()=>{gotoStep(1);setStatus("")});adminForm.addEventListener("submit",async e=>{e.preventDefault();const name=adminName.value.trim();const pass=adminPass.value.trim();if(!name||!pass){setStatus("请填写管理员用户名与密码","error");return}try{setStatus("创建管理员…");const resp=await fetch("/api/setup/admin",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({admin_username:name,admin_password:pass})});const d=await resp.json().catch(()=>({}));if(!resp.ok){setStatus("创建管理员失败："+(d.detail||d.error||resp.status),"error");return}setStatus("安装完成，正在跳转…","ok");setTimeout(()=>{location.href="/admin"},800)}catch(err){setStatus("请求失败","error")}});window.addEventListener("DOMContentLoaded",()=>{gotoStep(1);setStatus("")})})();